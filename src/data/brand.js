// One source of truth for the name and tagline, so the topbar, the footer, the
// exported file headers and the document title never drift apart.
// index.html carries its own copy because it is static — keep them in step.

export const BRAND_NAME = 'ANAM[QR]PHIC'
export const TAGLINE = '3D Printable Voxel Diorama QR code'
export const TAGLINE_FULL = `${BRAND_NAME} - ${TAGLINE}`
